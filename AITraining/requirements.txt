# ─── 3D-Reconstruction AI Server — Dependencies ───────────────────────────────
# Cài 1 lần duy nhất:
#   pip install -r AITraining/requirements.txt
#
# Với GPU CUDA 12.2 (khuyến nghị):
#   pip install llama-cpp-python[server] \
#       --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu122
#
# CPU only:
#   pip install llama-cpp-python

# ── LLM ────────────────────────────────────────────────────────────────────────
# llama-cpp-python cài thủ công theo hướng dẫn trên (GPU/CPU)
huggingface_hub>=0.23.0

# ── Embedding & RAG ────────────────────────────────────────────────────────────
sentence-transformers>=2.7.0
faiss-gpu>=1.8.0  
#faiss-cpu>=1.8.0              # Đổi thành faiss-gpu nếu muốn encode nhanh hơn
rank_bm25>=0.2.2
numpy>=1.26.0

# ── Document loaders ───────────────────────────────────────────────────────────
python-docx>=1.1.0
pdfplumber>=0.11.0
pdfminer.six>=20231228

# ── API server ─────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
